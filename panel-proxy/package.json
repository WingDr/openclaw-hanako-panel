{
  "name": "panel-proxy",
  "version": "0.1.0",
  "description": "Panel Proxy scaffold for LAN HTTP mode using Fastify and WebSocket",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "echo 'no tests yet'"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/websocket": "^8.3.1",
    "fastify": "^4.0.0",
    "ws": "^8.0.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  }
}
